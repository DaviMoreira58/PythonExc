#Utilizando Módulos

#Biblioteca 'math' - Funçoes ↓
# ceil - arredonda para cima
# floor - arredonda para baixo
# trunc - 'truncar' um numero, eliminar da "," para frente
# pow - potencia
# sqrt - raiz quadrada
# factorial - para calculo de fatorial
# Importar uma bliblioteca toda se não for usar todas as funçoes pode ser um problema
# pois ela ira ocupar memoria desnecessaria, por isso ten como fazer importações especificas
# import math - importa toda a biblioteca
# from math import sqrt - importa somente a sqrt
# from math import sqrt, ceil - importa somente as duas

#import math
#num = int(input('Digite um numero: '))
#raiz = math.sqrt(num)
#print('A raiz de {} é igual a {}'.format(num, raiz))
#print('A raiz de {} é igual a {}'.format(num, math.ceil(raiz))) # arredonda para cima
#print('A raiz de {} é igual a {}'.format(num, math.floor(raiz))) # arredonda para baixo

#import random # gera numeros aleatorios
#nume = random.random() # o "random" gera um numero real entre 0 e 1, se quiser um numero inteiro precisa de "randint(1, 10)" de um a 10 por exp
#print(nume)

